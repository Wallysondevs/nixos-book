# Demonstração {#sec-profile-demo}

Este perfil apenas habilita um usuário `demo`, com a senha `demo`, uid `1000`, grupo `wheel` e [autologin no gerenciador de exibição SDDM](#opt-services.displayManager.autoLogin).