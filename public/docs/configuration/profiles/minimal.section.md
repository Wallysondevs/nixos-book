# Mínimo {#sec-profile-minimal}

Este perfil define uma pequena configuração do NixOS. Ele não contém nenhum componente gráfico. É um arquivo muito curto que define os *locales* suportados para suportar apenas o *locale* selecionado pelo usuário, e [desabilita a documentação dos pacotes](#opt-documentation.enable).